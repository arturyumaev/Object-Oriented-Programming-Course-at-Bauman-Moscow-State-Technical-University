#include "MyVector.h"

/*
template <typename T> class MyVector
{
    private:
        T* _array;
        int _arraySize;
    public:
        MyVector();

        // Vector with size <size>, filled by <defaultValue>
        MyVector(__SIZE_TYPE__ num, const T& val = T());

        ~MyVector();

        void push_back();

        void pop_back();

        T* reference_back();

        T* reference_at(__SIZE_TYPE__ i);

        int size();

        int capacity();

        // Remove unusable memory
        void shrink_to_fit();

        bool empty();

        void clear();

        void reserve();

        void resize;

        T at(int i);
}; */
